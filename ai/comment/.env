# `.env` 文件是一种常见的配置文件格式，通常用于存储应用程序的环境变量。这些环境变量包括但不限于数据库连接信息、API密钥、端口号以及其他与应用程序相关的配置参数。使用 `.env` 文件有助于将敏感信息和配置分离出来，使得应用程序更易于管理和部署。

# `.env` 文件通常采用键值对的形式保存配置信息，例如：

# ```
# DB_HOST=localhost
# DB_USER=myuser
# DB_PASSWORD=mypassword
# API_KEY=abcdef123456
# ```

# 在应用程序中，可以通过读取 `.env` 文件来获取这些配置信息，并在运行时动态地应用到应用程序中。这样做有助于保护敏感信息，同时也方便了在不同环境中配置应用程序。

# 请注意，`.env` 文件通常应该包含在 `.gitignore` 等版本控制工具的忽略列表中，以避免将敏感信息提交到版本控制系统中，从而泄漏敏感数据。


OPENAI_API_KEY=sk-NuXTFda8FxDtb7VM1uLWia5vWl4kIERuMQksS2mQJxSblHEK