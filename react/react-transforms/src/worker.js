// NLP 任务中 pipeline 派出一个XX NLP任务 translate任务
import { pipeline } from '@xenova/transformers';
// 封装了翻译类
class MyTranslationPipeline{
  static task = 'translation'
  static model = 
  static 
}
self.addEventListener('message', function(e)
{
    console.log(e.data)
    self.postMessage('hello')
});